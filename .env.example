# Copy to .env (python-dotenv is loaded by the CLI) or export in your shell.
ANTHROPIC_API_KEY=sk-ant-...
# LLM_BACKEND=anthropic | ollama | cache-only | queue   (default: anthropic if key set, else ollama)
# LLM_MODEL=claude-haiku-4-5-20251001                  (ollama default: qwen3:1.7b)
# OLLAMA_HOST=http://localhost:11434
